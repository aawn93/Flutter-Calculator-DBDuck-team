import 'package:flutter/material.dart';

// ignore: constant_identifier_names
final ButtonStyle kMainNumButton_color = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xff202521), shape: const CircleBorder());

final ButtonStyle kMainOtherButton_color = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xff1A1C1A), shape: const CircleBorder());

final ButtonStyle kMainEqationButton_color = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xff2B352E), shape: const CircleBorder());

final ButtonStyle kMainEquleButton_color = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xff215135), shape: const CircleBorder());

final ButtonStyle kMainACButton_color = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xff2D4B58), shape: const CircleBorder());

const Color kMainCalScreen_BGColor = Color(0xff2B352E);

const TextStyle kMainButtonText_Style = TextStyle(
    fontSize: 26.0, fontWeight: FontWeight.bold, color: Color(0xffE0E2DE));

const TextStyle kMainBigSceen_Style = TextStyle(
    fontSize: 42.0, fontWeight: FontWeight.bold, color: Color(0xffE0E2DE));

const TextStyle kMainAnswerSceen_Style = TextStyle(
    fontSize: 32.0, fontWeight: FontWeight.bold, color: Color(0xffE0E2DE));

const Color mainBackgroundColor = Color(0xff1A1C1A);
